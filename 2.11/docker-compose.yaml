services:
  node-dev-env:
    build: .
    command: npm run dev
    ports: 
      - 3001:3001
    volumes:
      - ./:/usr/src/app
      - node_modules:/usr/src/app/node_modules
    container_name: node-dev-env

volumes:
  node_modules: