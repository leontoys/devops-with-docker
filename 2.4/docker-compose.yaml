services:
  redis:
    image : redis
  backend:
    image : backend
    ports:
      - 8080:8080
    environment:
      - REQUEST_ORIGIN=http://localhost:5001
      - REDIS_HOST=redis
  frontend:
    image : frontend
    ports:
      - 5001:5001
